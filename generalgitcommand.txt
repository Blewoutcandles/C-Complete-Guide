git remote add origin https://github.com/Blewoutcandles/your-repo.git   https do git push -u origin master or main
git remote add origin git@github.com:Blewoutcandles/your-repo.git       ssh setup standard add and commit, requires key to push
git push --set-upstream origin main                                     If there is no upstream branch to commit
git remote -v                                                           Know your remote name
git remote remove origin                                                Remove the remote repo from the current folder
git push origin --delete cpp_branch                                     Delete the cpp branch from remote
git push origin cpp_branch                                              Push the branch cpp_branch to the remote
git branch -d cpp_branch                                                Delete the local branch named cpp_branch
git rebase origin/main                                                  Rebases your current branch with files from remote branch main
git branch                                                              List branch
git config --global user.name "name"                                    Configure user name
git config --global user.name "email.com"                               Configure user email
git status                                                              Know the Status of the git
git log --oneline                                                       Know the logs with lines
git branch -M main                                                      Rename the head branch to main

*****************************************************************************************************************************

STEPS TO ADD SSH KEY TO GIT:
Perform these on terminal or bash or git bash:
git --version                                       make sure git is installed
ssh-keygen -t ed25519 -C "***@gmail.com"      Generate the ssh key, yes, enter passphrase
cat ~/.ssh/id_ed25519.pub                           View the public key, copy the key and goto settings in github, paste and save


Perform this on git bash or bash:
eval "$(ssh-agent -s)"                              Start the ssh agent
ssh-add ~/.ssh/id_ed25519                           Add the key, failing start bash as administrator

Perform Authentication:
ssh -T git@github.com                               Initiate a ssh connection to test, type yes
Hi your-username! You've successfully authenticated, but GitHub does not provide shell access.





